spring.application.name=schoolManagmentSystem

## MySQL connection
#spring.datasource.url=jdbc:mysql://localhost:3306/school_manager_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=12345


# C?u hình ?? ??c bi?n môi tr??ng t? Railway
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# B?t bu?c: B?o Spring Boot ch?y trên port mà Railway cung c?p
server.port=${PORT}

# JDBC driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Quan tr?ng: T? ??ng c?p nh?t schema khi deploy
# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.defer-datasource-initialization=true

#server.port=8080

